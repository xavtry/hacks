javascript:(async()=>{if(location.hostname!=='yohoho.io')return alert('Switch to https://yohoho.io first');const n=Number(prompt('Coins to add:'));if(!Number.isFinite(n)||n<0)return;let p=window.playerStats||window.saveData||{};p.coinsOwned=(p.coinsOwned||0)+n;window.coinsOwned=p.coinsOwned;const s=localStorage.sessionId;if(s){await fetch(`https://s.yohoho.io/save?s=${s}`,{method:'POST',body:JSON.stringify(p),headers:{'Content-Type':'application/json'}})}alert('Coins updated!')})();
